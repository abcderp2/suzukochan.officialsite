name: Site checks

on:
  pull_request:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: site-checks-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

defaults:
  run:
    shell: bash --noprofile --norc -euo pipefail {0}

jobs:
  check:
    runs-on: ubuntu-24.04
    timeout-minutes: 8
    steps:
      - name: Check out the triggering ref and reachable history without an external action
        env:
          REPOSITORY: ${{ github.repository }}
          REF: ${{ github.ref }}
          EXPECTED_SHA: ${{ github.sha }}
          SERVER_URL: ${{ github.server_url }}
        run: |
          umask 077
          git init .
          git remote add origin "${SERVER_URL}/${REPOSITORY}.git"
          git fetch --no-tags --prune origin \
            "${REF}" \
            '+refs/heads/*:refs/remotes/origin/*'
          git checkout --detach "${EXPECTED_SHA}"
          test "$(git rev-parse HEAD)" = "${EXPECTED_SHA}"

      - name: Enforce Actions security policy
        run: python3 -I scripts/workflow_policy.py

      - name: Check static site files
        run: |
          log_file="${RUNNER_TEMP}/site-check.log"
          status=0
          python3 -I scripts/check_site.py >"${log_file}" 2>&1 || status=$?

          escape_command() {
            local value="$1"
            value="${value//'%'/'%25'}"
            value="${value//$'\r'/'%0D'}"
            value="${value//$'\n'/'%0A'}"
            printf '%s' "${value}"
          }

          while IFS= read -r line; do
            case "${line}" in
              ERROR:\ *)
                message="${line#ERROR: }"
                printf '::error title=Site checks::%s\n' "$(escape_command "${message}")"
                ;;
              WARNING:\ *)
                message="${line#WARNING: }"
                printf '::warning title=Site checks::%s\n' "$(escape_command "${message}")"
                ;;
            esac
          done <"${log_file}"

          if [ "${status}" -eq 0 ]; then
            result="Passed"
          else
            result="Failed"
          fi

          {
            echo "## Site checks"
            echo
            echo "- Result: **${result}**"
            echo "- Commit: \`${GITHUB_SHA}\`"
            echo "- Command: \`python3 -I scripts/check_site.py\`"
            echo
            echo "### Output"
            sed 's/^/    /' "${log_file}"
          } >>"${GITHUB_STEP_SUMMARY}"

          cat "${log_file}"
          exit "${status}"

      - name: Reject unsupported static response-header meta
        run: python3 -I scripts/check_static_headers.py

      - name: Run the required security audit
        run: |
          log_file="${RUNNER_TEMP}/security-audit.log"
          status=0
          python3 -I scripts/security_audit.py --history >"${log_file}" 2>&1 || status=$?

          escape_command() {
            local value="$1"
            value="${value//'%'/'%25'}"
            value="${value//$'\r'/'%0D'}"
            value="${value//$'\n'/'%0A'}"
            printf '%s' "${value}"
          }

          while IFS= read -r line; do
            case "${line}" in
              ERROR:\ *)
                message="${line#ERROR: }"
                printf '::error title=Required security audit::%s\n' "$(escape_command "${message}")"
                ;;
            esac
          done <"${log_file}"

          if [ "${status}" -eq 0 ]; then
            result="Passed"
          else
            result="Failed"
          fi

          {
            echo "## Required security audit"
            echo
            echo "- Result: **${result}**"
            echo "- Commit: \`${GITHUB_SHA}\`"
            echo "- Current tree: checked"
            echo "- Reachable Git history: checked"
            echo "- Command: \`python3 -I scripts/security_audit.py --history\`"
            echo
            echo "### Output"
            sed 's/^/    /' "${log_file}"
          } >>"${GITHUB_STEP_SUMMARY}"

          cat "${log_file}"
          exit "${status}"
