# セキュリティ方針

## このサイトにAPIキーは不要です

鈴子ちゃん公式サイトは、HTML、CSS、画像をGitHub Pagesで配信する静的サイトです。OpenAI APIキーを含むAPIキー、アクセストークン、パスワード、クラウド認証情報は、開発、公開、保守のどの作業にも使用しません。

AIへ保守を依頼するときも、APIキーやパスワードを会話、Issue、Pull Request、コード、画像、設定ファイルへ貼り付けないでください。GitHub連携が必要な場合は、サービスの正式な権限画面から必要最小限の権限だけを許可します。

## 自動検査

Pull Requestとmainへのpushでは、次の読み取り専用検査を実行します。

- `python3 -I scripts/check_site.py`
- `python3 -I scripts/security_audit.py --history`

mainブランチの必須ステータス検査`check`は、上記2つを同じジョブ内で実行します。どちらか一方でも失敗した場合は必須検査が失敗し、マージできません。独立した`Security audit`ワークフローも追加確認として維持します。

`security_audit.py`は、現在のファイルと取得済みのGit履歴を調べ、OpenAI、GitHub、AWS、Google、Slack、npm、Stripeなどの既知の認証情報形式、秘密鍵、認証情報らしい代入、危険なファイル名を検出します。検出時は値を表示せず、種類、場所、照合用の短いハッシュだけを出力します。

外部Action、外部パッケージ、有料APIは使用しません。Python標準ライブラリとGitだけで動作します。

## 誤って秘密情報を公開した場合

ファイルを削除するだけでは、Git履歴やフォークに値が残る可能性があります。次の順序で対応します。

1. 該当サービスの管理画面で、漏えいしたキーやトークンを直ちに無効化します。
2. 必要な場合だけ新しい認証情報を発行し、公開リポジトリには保存しません。
3. 公開中のファイルから値を削除します。
4. Git履歴からの除去が必要か確認し、履歴を書き換える場合は事前にバックアップと影響範囲を確認します。
5. 自動検査を再実行し、同じ種類の値が残っていないことを確認します。

履歴を書き換えても、無効化していない認証情報の安全性は戻りません。最初に無効化します。

## 脆弱性の報告

秘密情報そのものを公開Issueへ書かないでください。GitHubのSecurityタブに非公開の脆弱性報告機能が表示される場合は、その機能を使用します。非公開報告が使えない場合は、公開Issueには再現に必要な最小限の情報だけを書き、キー、トークン、個人情報、未公開画像、悪用可能な詳細は伏せます。

対象は、このリポジトリの公開サイト、GitHub Actions、保守用検査コードです。外部SNS、GitHub、ブラウザ、端末、外部サービス自体の問題は各サービスの窓口へ報告します。

## 保守時の原則

- 変更は専用ブランチとPull Requestにまとめます。
- Actionsの権限は`contents: read`を基本とします。
- 外部Actionを追加せず、必要な処理は短い標準機能で実行します。
- JavaScript、外部CDN、分析タグ、広告、フォーム、バックエンド、API接続を安易に追加しません。
- 変更前のコミットを記録し、問題時はPull RequestのSquashコミットをRevertします。
