# 鈴子ちゃん公式サイト 保守マニュアル

この文書は、鈴子ちゃん公式サイトの保守に必要な事実、依頼文、作業手順、確認項目、復旧方法を一つに統合した正本です。

AIへ保守を依頼するときは、このファイルと今回の依頼内容を最初に渡してください。別の保守説明書を新しく作らず、手順を変更するときはこのファイルを更新します。

## 1. 想定する環境

一般的な無料プランのチャットAIまたはコーディングAIと、エントリークラスのスマートフォンまたはタブレットだけでも保守できることを前提とします。

GitHubアプリまたはスマートフォン向けブラウザを使用します。パソコン、ローカル開発環境、有料API、有料サービスは必須ではありません。

AIがGitHubを直接読み書きできる場合は、調査、変更、検査、プルリクエスト作成、公開確認、報告まで一続きで実行します。直接読み書きできない場合は、AIが変更対象、変更後の全文または差分、確認手順を提示し、権利者がGitHubのウェブ編集画面で反映します。

小さい画面と限られた通信量を前提に、AIは画像ファイルを読み込まず、最初にこの文書を読み、次に依頼へ関係するテキストファイルだけを確認します。巨大な一括出力を避け、ファイル名、変更理由、確認結果を短く区切って扱います。

## 2. AIへ渡す依頼文

次の依頼文に、今回の具体的な目的を追記して渡します。

```text
鈴子ちゃん公式サイトを保守してください。
正本は https://github.com/abcderp2/suzukochan.officialsite です。
公開サイトは https://abcderp2.github.io/suzukochan.officialsite/ です。
このリポジトリの MAINTENANCE.md を最優先で読み、現在の表示、文章、画像、軽量な静的サイト構成を維持してください。
今回の目的に必要な最小限だけを変更し、調査、変更、検査、プルリクエスト、公開確認、戻し方の報告まで進めてください。
今回の依頼内容:
ここへ依頼を書く
```

## 3. 正本と参照順位

正本リポジトリ

https://github.com/abcderp2/suzukochan.officialsite

公開サイト

https://abcderp2.github.io/suzukochan.officialsite/

公開元ブランチ

`main`

内容の参照順位は次のとおりです。

1. 公開中の文章、プロフィール、設定、ガイドラインは`index.html`
2. 権利条件は`LICENSE`と`index.html`内のガイドライン
3. 保守方法は`MAINTENANCE.md`
4. 保守方法と技術構成の変更履歴は`CHANGELOG.md`
5. リポジトリの概要は`README.md`

資料間に表記差がある場合、AIは推測で統一しません。権利者確認待ちとして報告します。

## 4. ファイル構成

主な公開ファイル

- `index.html`
- `en.html`
- `404.html`
- `assets/css/style.css`
- `assets/images/`
- `assets/images/white-wing-hospital/`

保守と検査のファイル

- `MAINTENANCE.md`
- `CHANGELOG.md`
- `README.md`
- `SECURITY.md`
- `scripts/check_site.py`
- `scripts/security_audit.py`
- `.github/workflows/`
- `.github/CODEOWNERS`
- `.gitignore`
- `.nojekyll`

このサイトはHTML、CSS、画像だけで公開する静的サイトです。JavaScript、Node.js、npm、フレームワーク、ビルド処理、データベース、バックエンドは使用しません。画像の拡大表示もCSSの`:target`だけで動作します。Pythonを使う検査は標準ライブラリだけで動作します。

### 画像のプライバシー保護

画像処理は鈴子ちゃん公式サイトでは行いません。追加画像をGitHubへ入れる前に、[画像メタデータクリーナー](https://abcderp2.github.io/Exif/)をスマートフォンまたはタブレットで開きます。元画像を選び、端末内で処理された画像を保存し、保存した画像だけをGitHubへアップロードします。元画像を先に公開しません。

JPEG、PNG、WebPに対応しています。`scripts/check_site.py`はJPEG、PNG、WebPに残ったExifなどのメタデータを検査し、検出した場合は検査を失敗させます。検査を通らない画像を手作業で無理に公開しません。

画像メタデータクリーナーは公式サイトへ埋め込まず、公式サイトの公開ページや更新情報にも追加しません。

### White Wing病院の公式背景ビジュアル

公開場所とファイル名は次のとおりです。

- `assets/images/white-wing-hospital/01_whitewing_hospital_exterior.webp`
- `assets/images/white-wing-hospital/02_whitewing_hospital_lobby.webp`
- `assets/images/white-wing-hospital/03_whitewing_hospital_corridor.webp`
- `assets/images/white-wing-hospital/04_whitewing_hospital_exam_room.webp`

画像はWebPを維持します。差し替える場合は、メタデータクリーナーで処理した画像だけを使用し、同じファイル名で置き換えるか、HTML、README、保守資料の参照を同時に更新します。

`index.html`と`en.html`は同じ画像を同じ順序で参照します。画像の実寸が変わった場合は、両方のHTMLにある`width`と`height`も実寸へ更新します。サムネイルは`object-fit: contain`で全体を表示するため、縦横比の違う画像を自動的に切り抜きません。

PCでは2列、幅700px以下では1列です。画像を押すと同じページ上へCSSだけで拡大表示します。拡大画像は完全なモーダルダイアログとは宣言せず、ページ内の名前付き領域として扱います。キーボードではサムネイルをEnterで開き、Tabで直後の閉じるリンクへ移動し、Enterで同じサムネイルへ戻ります。背景のタップでも同じサムネイルへ戻ります。外部スクリプト、JavaScriptライブラリ、画像CDNを追加しません。

公式名称は「White Wing Hospital」「ホワイトウィング病院」「White Wing病院」の3表記で、主に「White Wing病院」を使用します。青い翼と白い翼はどちらも公式シンボルです。公式背景ビジュアルには、公開ページの鈴子ちゃん利用ガイドラインと同じ条件を適用します。

### 外部サービスのロゴ

外部サービスへのリンクに使うロゴは、各社の公式ブランドページ、プレスキット、配布データを確認し、リンク先のサービスを示す用途に合うロゴを使用します。サービスロゴと企業ロゴが別に用意されている場合は混同しません。

ロゴを手書きや文字入力で作り直しません。色、形、縦横比を変えず、切り離し、影、縁取り、グラデーション、フィルター、回転、変形、他の図形との合成を行いません。公式な提携、支援、推奨があるように見せません。明るい背景と暗い背景の指定、最小サイズ、余白が示されている場合はその指定を優先します。

ロゴはリポジトリ内へ置き、外部CDN、外部スクリプト、埋め込みウィジェット、アクセス解析を追加しません。公式配布データまたは公式サービスが配信する同一データをそのまま使用し、形式変換や再トレースを避けます。差し替えるときは、取得元、確認日、用途、表示寸法をプルリクエスト本文へ残します。

現在の参照先は次のとおりです。参照先が移動した場合は、各サービスの公式サイトから最新のブランドページを探します。

- YouTube: https://brand.youtube/
- note: https://www.help-note.com/hc/ja/articles/360000235582
- X: https://about.x.com/ja/who-we-are/brand-toolkit
- pixiv: https://www.pixiv.co.jp/news/press-kit/
- GitHub: https://brand.github.com/foundations/logo

pixivは明るいカード上に青い現行サービスロゴを表示します。ロゴ単体のカードで表示寸法を調整し、画像の縦横比は`object-fit: contain`で維持します。日本語版と英語版は同じロゴファイル、リンク先、表示条件を使います。

## 5. AIによる読み取り、解析、学習

ウェブサイトのコードと保守資料であるHTML、CSS、Python、YAML、Markdownは、AIによる読み取り、解析、要約、学習用の参照、保守、改善提案への利用を歓迎します。コードを複製、変更、配布するときの条件は`LICENSE`内のMIT Licenseに従います。

鈴子ちゃんのキャラクター、設定、イラスト、White Wing病院の公式背景ビジュアルはコードとは別の条件です。公開ページのガイドラインと`LICENSE`に従います。公式画像ファイルをそのまま、または実質的に同一の状態で転載・再配布することは禁止します。公式画像を参考にしたり制作過程で利用したりした二次創作作品は、ガイドラインの範囲で公開できます。個人の端末内での画像利用や追加学習モデルの作成と利用は現行ガイドラインの範囲で扱い、商用利用、年齢制限作品、なりすまし、学習モデルの公開・共有・配布・販売は行いません。White Wing病院の公式背景ビジュアルにも同じ区分を適用します。

コードの学習歓迎を、キャラクター画像や設定の無制限な利用許諾と解釈しません。

## 6. 最優先方針

現在正常に表示されているサイトを大きく作り直しません。

依頼された目的に必要な最小限のファイルと行だけを変更します。依頼と関係のない整形、言い換え、並べ替え、最適化、リファクタリングを混ぜません。

追加課金が必要なサービス、有料API、外部ホスティング、外部CMS、外部CDN、アクセス解析、広告、Cookie、外部フォーム、外部フォント、外部ライブラリを追加しません。

パスワード、個人アクセストークン、APIキー、二段階認証コード、復旧コード、秘密鍵、メール認証コードを要求、記録、表示、コメント、コミットしません。

AIは提案だけで止まらず、利用できる権限とツールの範囲でゴールまで進めます。ただし、検査に失敗した変更や、権利者の判断が必要な変更はmainへ反映しません。

## 7. 変更できる範囲

権利者から保守依頼を受けたAIは、次の作業を変更まで実行できます。

- 保守資料とチェック内容の整理
- 404ページなど補助ページの修正
- 存在しないローカル参照の修正
- 明らかなURL誤記の修正
- alt属性、noopener、noreferrerなどの不足を補う機械的修正
- Markdown、JSON、YAML、HTML、CSS、Pythonの単純な構文不良の修正
- 読み取り専用の自動検査の改善
- 読み取り専用のGitHub Actionsの改善
- 表示や意味を変えないアクセシビリティとセキュリティの修正

次は、権利者が対象と内容を明示した場合だけ変更します。不明な場合は変更せず、確認事項として報告します。

- 鈴子ちゃんの設定、プロフィール、本文の意味
- White Wing病院の名称、設定、シンボル、背景画像
- 利用ガイドライン
- `LICENSE`と権利表記
- 画像の追加、削除、差し替え、加工、変換、圧縮、リサイズ、切り抜き、ファイル名変更
- デザイン、色、余白、文字サイズ、配置、レスポンシブ表示、CSS
- `robots.txt`、`sitemap.xml`
- GitHub Pages設定、リポジトリ設定、ブランチ保護
- 外部公開先、公式URL、公式SNS

## 8. AIが最初に行う監査

1. 最新のmainと未処理のプルリクエストを確認します。
2. `MAINTENANCE.md`、`README.md`、`LICENSE`、`scripts/check_site.py`、`.github/workflows/check-site.yml`を確認します。
3. 今回の依頼に関係するファイルだけを追加で確認します。
4. 公開ファイルを変更する必要があるかを判定します。必要がなければ`index.html`、`en.html`、`404.html`、CSS、画像へ触れません。
5. 重複した説明、古い例外、存在しない参照、秘密情報、外部依存、表示変更の混入を確認します。
6. 変更対象と変更しない対象を確定します。

無料プランのAIで文脈量が不足する場合は、画像や全履歴を読み込まず、ファイル単位で確認します。推測で不足部分を補いません。

## 9. 標準の作業手順

1. 最新のmainを取得します。
2. 変更前のmainを指す復旧用ブランチを作成します。
3. 同じmainから一時作業ブランチを作成します。
4. 依頼された範囲だけを変更します。
5. `python3 -I scripts/check_site.py`を実行します。
6. `python3 -I scripts/security_audit.py --history`を実行します。
7. 差分を確認し、依頼外の変更と意図しない公開表示の変更がないことを確認します。
8. 一つの目的だけを含むプルリクエストを作成します。
9. GitHub Actionsの検査が成功したことを確認します。
10. 原則としてSquash and mergeを使い、mainへ一つのコミットとして反映します。
11. GitHub Pagesと公式サイトを確認します。
12. 変更内容、検査結果、mainのコミット、復旧用ブランチ、公開結果、戻し方を報告します。

一時作業ブランチに複数コミットがあっても、mainでは一つのSquashコミットにまとめます。これにより、一回のRevertで今回の変更全体を戻せます。

AIがプルリクエストを作れず、権利者がmainへの直接反映を明示した場合だけ、復旧用ブランチを作成し、事前検査に成功した変更を一つのコミットとしてmainへ反映します。

## 10. ブランチ名

復旧用ブランチ

`backup/YYYYMMDD-HHMM-目的`

一時作業ブランチ

`work/YYYYMMDD-HHMM-目的`

既存ブランチを上書きしません。force pushと履歴の削除を行いません。

## 11. スマートフォンまたはタブレットでの操作

GitHubアプリまたはブラウザでリポジトリを開きます。

AIがGitHubへ直接反映できない場合は、最新のmainから作業ブランチを作り、対象ファイルをウェブ編集します。AIにはファイル名と変更後の全文を出力させ、別のファイルへ誤って貼り付けないよう一ファイルずつ保存します。

プルリクエストを開き、次を確認します。

- Files changedが依頼したファイルだけになっている
- 画像、CSS、公開本文を変更しない依頼では、それらが差分に含まれていない
- 画像変更ではメタデータ、容量、形式、実寸とHTMLの`width`、`height`を確認している
- White Wing病院の画像はPCで2列、狭い画面で1列になり、拡大表示と閉じる操作が動く
- ロゴ変更では公式配布元、縦横比、背景とのコントラストを確認している
- ChecksまたはActionsが成功している
- 追加課金、外部依存、認証情報が差分に含まれていない
- mainへの反映方法がSquash and mergeになっている

反映後に公式サイトを開き、トップページ、画像、リンク、スマートフォン幅、404ページを確認します。

公式サイト

https://abcderp2.github.io/suzukochan.officialsite/

404確認用URL

https://abcderp2.github.io/suzukochan.officialsite/not-found-check

ローカル確認は任意です。端末性能や環境の都合で実行できない場合は、GitHub Actionsと公開サイトの確認を使用します。

## 12. 検査

標準検査

```bash
python3 -I scripts/check_site.py
python3 -I scripts/security_audit.py --history
```

`-I`はPythonを隔離モードで実行し、利用者環境の設定や追加パッケージの影響を減らします。

検査スクリプトは読み取り専用です。ファイルの自動修正、コミット、プッシュ、マージを行いません。エラーが一件でもあれば終了コード1を返し、mainへ反映しません。

ローカル表示確認を行う場合だけ、次を使用します。

```bash
python3 -m http.server 8000
```

ブラウザで`http://localhost:8000/`を開きます。終了するときは`Ctrl+C`を使用します。

## 13. 反映前チェックリスト

- 最新のmainを基準にしている
- 復旧用ブランチがある
- 依頼されたファイルだけを変更している
- 本文、画像、ガイドライン、LICENSE、権利表記を無断変更していない
- 日本語版と英語版で画像参照、スタイルシート、外部リンクが一致している
- White Wing病院の公式名称3種、主表記、翼の色、画像利用条件が依頼内容と一致している
- 画像の形式、容量、メタデータ、実寸、alt、遅延読み込みが適切である
- PC、タブレット、スマートフォンで横スクロール、重なり、画像のはみ出しがない
- キーボードでサムネイルと閉じるリンクへ移動できる
- 有料サービス、外部API、外部依存を追加していない
- 認証情報が含まれていない
- `python3 -I scripts/check_site.py`が成功している
- `python3 -I scripts/security_audit.py --history`が成功している
- 差分に競合記号が残っていない
- プルリクエスト本文に変更理由、表示への影響、検査、戻し方がある

## 14. 反映後チェックリスト

- GitHub Actionsが成功している
- トップページと英語ページが表示される
- CSSと全画像が表示される
- White Wing病院が鈴子ちゃんについてと更新情報の間にある
- PCで2列、スマートフォンで1列になる
- 4枚すべての拡大表示、閉じるリンク、背景タップが動く
- ページ内リンクと外部リンクが動作する
- スマートフォン幅で横スクロール、重なり、画像のはみ出しがない
- 存在しないURLで404ページが表示される
- main上のRevert対象コミットを記録した
- 復旧用ブランチを公開確認が終わるまで残している

## 15. 失敗時と元に戻す方法

マージ前に問題が見つかった場合は、プルリクエストを閉じます。mainは変更されません。

事前検査に失敗した場合はmainへ反映せず、同じ作業ブランチで修正して再検査します。

マージ後に検査または公開確認が失敗した場合は、対象のSquashコミットをRevertするコミットを作成し、再度検査と公開確認を行います。

復旧用ブランチは変更前のmainを指すだけのブランチで、公開には使用しません。復旧用ブランチでmainを強制上書きしません。

Revertが競合する場合は、復旧用ブランチと現在のmainを比較し、今回の変更だけを打ち消す新しいコミットを作成します。

force push、hard reset、履歴削除は使用しません。

## 16. プルリクエスト本文

プルリクエスト本文には次を記載します。

- 変更した内容
- 変更理由
- 公開表示への影響
- 変更したファイル
- 変更しなかった公開ファイル
- 画像変更では元データ、処理方法、形式、実寸、容量、メタデータ検査
- ロゴ変更では公式配布元と確認日
- 実行した検査と結果
- 未解決事項
- 元へ戻す方法

## 17. 完了報告

完了時は次を報告します。

- 変更したファイル
- 変更理由
- コード監査で見つけた事項と対応
- 実行した検査と結果
- mainへ反映したコミット
- 復旧用ブランチ
- 公開確認の結果
- 未解決事項
- Revert対象

## 18. この文書の維持

保守手順、正本情報、チェックリスト、AI向け依頼文はこのファイルだけに置きます。READMEには概要とこのファイルへの案内を置きます。

同じ手順を別ファイルへ複製しません。保守方法や技術構成の意味が変わる場合だけ`CHANGELOG.md`へ記録し、既存方針の補足、公式参照先の更新、重複説明の統廃合はこのファイルだけで整理します。
